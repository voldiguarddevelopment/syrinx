# spec.md — superseded

This file was a **byte-identical copy** of [`plan.md`](plan.md) (md5
`12c5b94274f48442f0aed4ca8bf96f53`). Two identical files diverge silently the first time
someone edits one of them, so the content now lives in `plan.md` alone.

`CLAUDE.md` still names a `plan.md`/`spec.md`/`list.md` trio because that is what the
Ratchet loop consumed. That loop is finished: all 92 tasks are `done`, `blocked`, or
`split`, and none is `open`. Current work is tracked in
[`docs/upgrades/SYRINX_UPGRADE_expressive_control.md`](docs/upgrades/SYRINX_UPGRADE_expressive_control.md).
